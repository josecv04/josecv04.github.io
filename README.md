# Jose Resume Site (Final Polish)

## Deploy (GitHub Pages)
Repo → Settings → Pages → Deploy from branch `main` / folder `/ (root)`.

## Add visuals (recommended)
Put images in `assets/` and name them:
- `legoeeg.png`
- `microneedle.png`
- `robocar.png`
- `ecg-epg.png`

If you upload images here, I can patch the HTML to swap the placeholder boxes for <img> tags.
